# installer-guidato — task2 — Worklog

## Avanzamento
DA TESTARE

- [x] Impianto dello script: shebang, intestazione, calcolo della radice, funzioni di stampa esiti e accumulatore dei problemi
- [x] Controlli preliminari: presenza di `main.py`, `requirements.txt` e `core/` nella radice
- [x] Controllo di Python: versione minima 3.9 e disponibilità del modulo `venv`
- [x] Controllo e installazione dei pacchetti di sistema (`ffmpeg`, `git`, `python3-venv`) tramite `apt`
- [x] Creazione della venv e installazione di `requirements.txt`
- [x] Creazione delle cartelle `data/` e `temp/`
- [x] Creazione guidata di `data/.env` se manca, con permessi restrittivi; sola verifica se esiste
- [x] Verifiche finali: `MUSIC_DIR` scrivibile, ffmpeg funzionante, import di `aiogram` e `yt_dlp`
- [x] Riepilogo conclusivo con esiti, passi rimanenti, avviso sul polling singolo e codice di uscita
- [x] Verifica di sintassi con `bash -n` senza esecuzione

## Test
_Da compilare dopo i test dell'utente._
